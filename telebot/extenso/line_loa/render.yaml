# render.yaml
services:
  - type: web
    name: extenso-village-portal
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --workers 4 wsgi:application"
    plan: starter